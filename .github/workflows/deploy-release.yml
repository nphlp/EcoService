name: Deploy Release
description: On PR merge to main branch, create a tag and semantic release.

on:
    push:
        branches: [main]

jobs:
    tag:
        name: Tag release
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            # - name: Tag release
            #   run: pnpm tag

    release:
        name: Release
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            # - name: Release
            #   run: pnpm release
